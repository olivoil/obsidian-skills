interface:
  display_name: "Intervals Time Entry"
  short_description: "Prepare and fill Intervals time entries for manual review and manual submit."
  default_prompt: "Use this skill to prepare and fill Intervals time entries from the vault, but always show the proposed hours first and never submit them."

policy:
  allow_implicit_invocation: false
